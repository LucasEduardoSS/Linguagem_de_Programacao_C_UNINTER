#include <stdio.h>

int potencia(int x, int y){
    if (y < 2) {
        return x;
    }
    else {
        return x * potencia(x, y - 1);
    }
};

int main() {
    int b = 3, e = 8;
    
    printf("%i", potencia(b, e));
    
    return 0;
}
