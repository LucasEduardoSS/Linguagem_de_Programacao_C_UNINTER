#include <stdio.h>
#include <stdlib.h>

#define maior(x, y) x<y?x:y

int main() {
    int a = 12;
    int b = 6;
    int c = maior(a, b);
    
    printf("Maior valor = %d\n\n", c);
    
    return 0;
}
