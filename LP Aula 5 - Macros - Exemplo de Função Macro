/* Figura 21 - Exemplo de uso de diretiva para definir uma função macro
que será subtituida em todas as chamadas pelo código e que verifica o
maior valor entre dois atribuidos 8/

#include <stdio.h>
#include <stdlib.h>

#define maior(x, y) x<y?x:y

int main() {
    int a = 12;
    int b = 6;
    int c = maior(a, b);
    
    printf("Maior valor = %d\n\n", c);
    
    return 0;
}
